export const metadata = {
    title: "help sleep mp3 |Background music|Best noise for sleep ",
    description: "Discover the best noise for sleep, from white noise to nature sounds, and find the best sleep noise to help you fall asleep faster and wake up refreshed.",
};
export default function AsmrLayout({children}) {
    return children;
}
